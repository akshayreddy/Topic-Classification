To run the program:

./topics train dataset-directory model-file [fraction]
./topics test dataset-directory model-file [fraction]

fraction:

1 for fully suprivised
0.5 for semi suprivised
0 for unsuprivised 

dataset-directory: is the path containing both the test and train folders.



